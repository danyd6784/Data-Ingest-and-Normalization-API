# Data-Ingest-and-Normalization-API
API that allows you to ingest messy xml, csv, and JSON data, normalize it and stores it in a postgres database.


### Design Decisions
* Decided to make a super basic front end document for easy data ingestion and adds a basic layer of protection against the wrong kind of inputs by using the "accepts" attribute of the input html tag

